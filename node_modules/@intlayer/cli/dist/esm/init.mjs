import { existsSync } from "node:fs";
import { join, resolve } from "node:path";
import { initIntlayer } from "@intlayer/chokidar/cli";

//#region src/init.ts
const findProjectRoot = (startDir) => {
	let currentDir = startDir;
	while (currentDir !== resolve(currentDir, "..")) {
		if (existsSync(join(currentDir, "package.json"))) return currentDir;
		currentDir = resolve(currentDir, "..");
	}
	return startDir;
};
const init = async (projectRoot, options) => {
	await initIntlayer(projectRoot ? findProjectRoot(resolve(projectRoot)) : findProjectRoot(process.cwd()), options);
};

//#endregion
export { findProjectRoot, init };
//# sourceMappingURL=init.mjs.map