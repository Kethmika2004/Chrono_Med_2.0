Object.defineProperty(exports, Symbol.toStringTag, { value: 'Module' });
const require_runtime = require('../_virtual/_rolldown/runtime.cjs');
const require_utils_getExtension = require('./getExtension.cjs');
const require_utils_normalizePath = require('./normalizePath.cjs');
let node_path = require("node:path");

//#region src/utils/alias.ts
const getAlias = ({ configuration, format, formatter = (value) => value }) => {
	const extension = require_utils_getExtension.getExtension(configuration, format ?? configuration.build.outputFormat[0] ?? "esm");
	const { baseDir } = configuration.system;
	const { mainDir, configDir } = configuration.system;
	return {
		"@intlayer/dictionaries-entry": formatter(require_utils_normalizePath.normalizePath((0, node_path.relative)(baseDir, (0, node_path.join)(mainDir, `dictionaries.${extension}`)))),
		"@intlayer/unmerged-dictionaries-entry": formatter(require_utils_normalizePath.normalizePath((0, node_path.relative)(baseDir, (0, node_path.join)(mainDir, `unmerged_dictionaries.${extension}`)))),
		"@intlayer/remote-dictionaries-entry": formatter(require_utils_normalizePath.normalizePath((0, node_path.relative)(baseDir, (0, node_path.join)(mainDir, `remote_dictionaries.${extension}`)))),
		"@intlayer/dynamic-dictionaries-entry": formatter(require_utils_normalizePath.normalizePath((0, node_path.relative)(baseDir, (0, node_path.join)(mainDir, `dynamic_dictionaries.${extension}`)))),
		"@intlayer/fetch-dictionaries-entry": formatter(require_utils_normalizePath.normalizePath((0, node_path.relative)(baseDir, (0, node_path.join)(mainDir, `fetch_dictionaries.${extension}`)))),
		"@intlayer/config/built": formatter(require_utils_normalizePath.normalizePath((0, node_path.relative)(baseDir, (0, node_path.join)(configDir, `configuration.${extension}`))))
	};
};

//#endregion
exports.getAlias = getAlias;
//# sourceMappingURL=alias.cjs.map